anzahl_mitarbeiter = 113
anzahl_mitarbeiter += 2 	#115
anzahl_mitarbeiter /= 2 	#56.5


```
In Python, the // symbol is the floor division operator, which divides two numbers and rounds the result down to the nearest whole number (integer).
Unlike the standard division operator (/), which always yields a floating-point number,
floor division chops off the fractional part and yields a whole number result.
Standard vs. Floor DivisionStandard Division (/):
7 / 2 equals 3.5.
Floor Division (//): 7 // 2 equals 3.
```

print(10 // 3)  # Output: 3  (10 / 3 is 3.333...)
print(5 // 2)   # Output: 2  (5 / 2 is 2.5)
